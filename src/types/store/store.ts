/**
 * ```typescript
 * interface IUserState {
    id: number;
    username: string;
}
 * ```
 * Holds information about the user state.
 */
export interface IUserState {
  id: number;
  username: string;
}